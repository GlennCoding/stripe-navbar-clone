import { Story, Meta } from '@storybook/react/types-6-0';
import ArrowForward from './index';

export default {
  component: ArrowForward,
  title: 'ArrowForward',
} as Meta;

export const Default: Story = () => <ArrowForward />;
